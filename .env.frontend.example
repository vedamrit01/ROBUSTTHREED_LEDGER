# Public configuration only. Copy to .env.local for a local frontend override.
# Local development uses Vite's /api proxy automatically.
VITE_API_URL=
# The GitHub Actions workflow supplies the correct repository path.
VITE_BASE_PATH=/
